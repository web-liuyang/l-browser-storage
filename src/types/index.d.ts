export namespace Local {

  interface IOptions {
    /**
     * 是否进行加密
     */
    encode?: boolean;

    /**
     * 是否进行解密
     */
    decode?: boolean;
  }
}

export namespace Session {
  
  interface IOptions {
    /**
     * 是否进行加密
     */
    encode?: boolean;

    /**
     * 是否进行解密
     */
    decode?: boolean;
  }
}